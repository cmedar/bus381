"""
Delta Lake helpers — table initialisation and upsert utilities.
Run on Databricks (not on EC2).
"""
# TODO: add init_bronze_table(), init_silver_table(), init_gold_table()
# TODO: add upsert_session() for Silver — merge on session_id
